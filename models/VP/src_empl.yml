version: 2

sources:
  - name: VP
    database: PC_DBT_DB
    schema: PUBLIC
    tables:
      - name: EMPLOYEES_SAL_RANGE
      - name: EMPLOYEES_SAMPLE
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}